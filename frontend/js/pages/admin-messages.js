import '../components/site-header.js';
import '../components/site-footer.js';

const currentUserId = 'admin-001';
const conversations = [
  { conversationId: 'A001', participantName: 'Nguyễn Minh Anh', participantRole: 'Khách hàng', latestMessage: 'Tôi cần hỗ trợ về lịch đặt sân.', latestMessageAt: '11:05', unreadCount: 3, context: ['Tài khoản KH-001', 'BK-240926-18', 'Sân Bách Khoa'], messages: [{ senderId: 'customer-001', content: 'Tôi cần hỗ trợ về lịch đặt sân.', sentAt: '11:05' }, { senderId: currentUserId, content: 'Tôi đã tiếp nhận yêu cầu của bạn.', sentAt: '11:10' }] },
  { conversationId: 'A002', participantName: 'Sân Bách Khoa', participantRole: 'Quản lý sân', latestMessage: 'Đã gửi thông tin bổ sung cho yêu cầu hỗ trợ.', latestMessageAt: '10:12', unreadCount: 1, context: ['Tài khoản QL-001', 'Sân Bách Khoa', 'Báo cáo RP-004'], messages: [{ senderId: 'manager-001', content: 'Đã gửi thông tin bổ sung cho yêu cầu hỗ trợ.', sentAt: '10:12' }] },
  { conversationId: 'A003', participantName: 'Trần Hoàng Nam', participantRole: 'Khách hàng', latestMessage: 'Cảm ơn đội hỗ trợ.', latestMessageAt: 'Hôm qua', unreadCount: 0, context: ['Tài khoản KH-002', 'BK-240925-20', 'Sân Tây Hồ Arena'], messages: [{ senderId: currentUserId, content: 'Yêu cầu của bạn đã được ghi nhận.', sentAt: 'Hôm qua' }, { senderId: 'customer-002', content: 'Cảm ơn đội hỗ trợ.', sentAt: 'Hôm qua' }] },
];

const root = document.querySelector('[data-messages-app]'); const list = root?.querySelector('[data-list]'); const empty = root?.querySelector('[data-empty]'); const view = root?.querySelector('[data-view]'); let selectedId = new URLSearchParams(location.search).get('conversationId');
const initials = name => name.split(/\s+/).map(word => word[0]).slice(0, 2).join('').toUpperCase(); const text = (element, value) => { element.textContent = value; };
function renderList() { const query = root.querySelector('[data-search]').value.trim().toLowerCase(); const role = root.querySelector('[data-role-filter]').value; const visible = conversations.filter(item => (role === 'all' || item.participantRole === (role === 'customer' ? 'Khách hàng' : 'Quản lý sân')) && `${item.participantName} ${item.latestMessage} ${item.context.join(' ')}`.toLowerCase().includes(query)); text(root.querySelector('[data-count]'), String(visible.length)); list.replaceChildren(); if (!visible.length) { const state = document.createElement('p'); state.className = 'messages-state'; text(state, 'Không có cuộc trò chuyện phù hợp.'); list.append(state); return; } visible.forEach(item => { const button = document.createElement('button'); button.type = 'button'; button.className = `conversation-item${item.conversationId === selectedId ? ' is-active' : ''}`; const avatar = document.createElement('span'); avatar.className = 'avatar'; avatar.setAttribute('aria-hidden', 'true'); text(avatar, initials(item.participantName)); const body = document.createElement('span'); body.className = 'conversation-item__body'; const top = document.createElement('span'); top.className = 'conversation-item__top'; const name = document.createElement('span'); name.className = 'conversation-item__name'; text(name, item.participantName); const time = document.createElement('time'); time.className = 'conversation-item__time'; text(time, item.latestMessageAt); top.append(name, time); const bottom = document.createElement('span'); bottom.className = 'conversation-item__bottom'; const preview = document.createElement('span'); preview.className = 'conversation-item__preview'; text(preview, item.latestMessage); bottom.append(preview); body.append(top, bottom); button.append(avatar, body); if (item.unreadCount) { const unread = document.createElement('span'); unread.className = 'conversation-item__unread'; text(unread, String(item.unreadCount)); button.append(unread); } button.addEventListener('click', () => selectConversation(item.conversationId)); list.append(button); }); }
function renderMessages(item) { const history = root.querySelector('[data-history]'); history.replaceChildren(); item.messages.forEach(message => { const article = document.createElement('article'); article.className = `message${message.senderId === currentUserId ? ' is-outgoing' : ''}`; const content = document.createElement('p'); content.className = 'message__content'; text(content, message.content); const time = document.createElement('time'); time.className = 'message__time'; text(time, message.sentAt); article.append(content, time); history.append(article); }); history.scrollTop = history.scrollHeight; }
function showEmpty() { root.classList.remove('has-selection'); empty.hidden = false; view.hidden = true; root.querySelector('[data-error]').hidden = true; renderList(); }
function selectConversation(id) { const item = conversations.find(value => value.conversationId === id); if (!item) return showEmpty(); selectedId = id; item.unreadCount = 0; root.classList.add('has-selection'); empty.hidden = true; view.hidden = false; root.querySelector('[data-error]').hidden = true; text(root.querySelector('[data-avatar]'), initials(item.participantName)); text(root.querySelector('[data-name]'), item.participantName); text(root.querySelector('[data-role]'), item.participantRole); const context = root.querySelector('[data-context]'); context.replaceChildren(...item.context.map(value => { const chip = document.createElement('span'); chip.className = 'context-chip'; text(chip, value); return chip; })); renderMessages(item); renderList(); }
function showLoad() { list.replaceChildren(); const state = document.createElement('p'); state.className = 'messages-state'; text(state, 'Đang tải cuộc trò chuyện…'); list.append(state); window.setTimeout(() => { renderList(); if (conversations.some(item => item.conversationId === selectedId)) selectConversation(selectedId); else showEmpty(); }, 120); }
root?.querySelector('[data-search]')?.addEventListener('input', renderList); root?.querySelector('[data-role-filter]')?.addEventListener('change', renderList); root?.querySelector('[data-back]')?.addEventListener('click', showEmpty); root?.querySelector('[data-retry]')?.addEventListener('click', showLoad); root?.querySelector('[data-composer]')?.addEventListener('submit', event => { event.preventDefault(); const textarea = event.currentTarget.querySelector('textarea'); const content = textarea.value.trim(); if (!content || !selectedId) return; const item = conversations.find(value => value.conversationId === selectedId); if (!item) return; item.messages.push({ senderId: currentUserId, content, sentAt: new Date().toLocaleTimeString('vi-VN', { hour: '2-digit', minute: '2-digit' }) }); item.latestMessage = content; item.latestMessageAt = 'Vừa xong'; textarea.value = ''; renderMessages(item); renderList(); }); showLoad();
